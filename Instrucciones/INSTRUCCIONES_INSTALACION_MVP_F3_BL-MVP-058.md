# Instalación BL-MVP-058

Base Git exacta requerida:

`dc7f19caecc43f770c6fbf55ef750cbd529d9a84`

1. Extraer el ZIP en la raíz de `SistemaMusicaYAprender`.
2. Desbloquear scripts PowerShell.
3. Ejecutar `scripts/apply-bl-mvp-058.ps1`.
4. No hacer `git add`, commit ni push.
5. Después de GREEN local, reiniciar el stack normal y revisar el borrador real en `/editorial/canciones/{recordingId}/sincronizacion`; no publicar ni inventar un slug.

La validación incluye carga diferida, `youtube-nocookie.com`, `origin`, eventos permitidos, degradación externa, independencia del contenido propio, ausencia de Data API, axe y 320 px.

No se requiere publicación para la revisión visual de BL-MVP-058.
